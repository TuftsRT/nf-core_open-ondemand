<%
require "json"

to_bool = lambda do |value|
  case value
  when true, false
    value
  when String
    normalized = value.strip.downcase
    next true if %w[true 1 yes y on].include?(normalized)
    next false if %w[false 0 no n off].include?(normalized)
    value
  else
    value
  end
end

to_number = lambda do |value|
  case value
  when Integer, Float
    value
  when String
    stripped = value.strip
    next value if stripped.empty?
    next stripped.to_i if stripped.match?(/\A[+-]?\d+\z/)
    next stripped.to_f if stripped.match?(/\A[+-]?(?:\d+\.?\d*|\.\d+)(?:[eE][+-]?\d+)?\z/)
    value
  else
    value
  end
end

params = {
    "input": context.input,
    "outdir": context.outdir,
    "multiqc_title": context.multiqc_title,
    "project_title": context.project_title,
    "genome": context.genome,
    "gene_annotation": context.gene_annotation,
    "hpo_file": context.hpo_file,
    "random_seed": to_bool.call(context.random_seed),
    "ec_gene_annotations": context.ec_gene_annotations,
    "ec_exclude_groups": context.ec_exclude_groups,
    "ae_skip": to_bool.call(context.ae_skip),
    "ae_groups": context.ae_groups,
    "ae_min_ids": to_number.call(context.ae_min_ids),
    "ae_fpkm_cutoff": to_number.call(context.ae_fpkm_cutoff),
    "ae_implementation": context.ae_implementation,
    "ae_padj_cutoff": to_number.call(context.ae_padj_cutoff),
    "ae_z_score_cutoff": to_number.call(context.ae_z_score_cutoff),
    "ae_max_tested_dimension_proportion": to_number.call(context.ae_max_tested_dimension_proportion),
    "ae_yield_size": to_number.call(context.ae_yield_size),
    "ae_genes_to_test": context.ae_genes_to_test,
    "ae_use_grid_search_to_obtain_q": to_bool.call(context.ae_use_grid_search_to_obtain_q),
    "as_skip": to_bool.call(context.as_skip),
    "as_groups": context.as_groups,
    "as_min_ids": to_number.call(context.as_min_ids),
    "as_recount": to_bool.call(context.as_recount),
    "as_long_read": to_bool.call(context.as_long_read),
    "as_keep_non_standard_chrs": to_bool.call(context.as_keep_non_standard_chrs),
    "as_skip_filter": to_bool.call(context.as_skip_filter),
    "as_min_expression_in_one_sample": to_number.call(context.as_min_expression_in_one_sample),
    "as_quantile_min_expression": to_number.call(context.as_quantile_min_expression),
    "as_min_delta_psi": to_number.call(context.as_min_delta_psi),
    "as_implementation": context.as_implementation,
    "as_fraser_version": context.as_fraser_version,
    "as_delta_psi_cutoff": to_number.call(context.as_delta_psi_cutoff),
    "as_quantile_for_filtering": to_number.call(context.as_quantile_for_filtering),
    "as_padj_cutoff": to_number.call(context.as_padj_cutoff),
    "as_max_tested_dimension_proportion": to_number.call(context.as_max_tested_dimension_proportion),
    "as_genes_to_test": context.as_genes_to_test,
    "as_use_grid_search_to_obtain_q": to_bool.call(context.as_use_grid_search_to_obtain_q),
    "mae_skip": to_bool.call(context.mae_skip),
    "mae_groups": context.mae_groups,
    "ucsc_fasta": context.ucsc_fasta,
    "ucsc_fai": context.ucsc_fai,
    "ucsc_dict": context.ucsc_dict,
    "ncbi_fasta": context.ncbi_fasta,
    "ncbi_fai": context.ncbi_fai,
    "ncbi_dict": context.ncbi_dict,
    "mae_gatk_header_check": to_bool.call(context.mae_gatk_header_check),
    "mae_padj_cutoff": to_number.call(context.mae_padj_cutoff),
    "mae_allelic_ratio_cutoff": to_number.call(context.mae_allelic_ratio_cutoff),
    "mae_add_af": to_bool.call(context.mae_add_af),
    "mae_max_af": to_number.call(context.mae_max_af),
    "mae_max_var_freq_cohort": to_number.call(context.mae_max_var_freq_cohort),
    "mae_qc_vcf": context.mae_qc_vcf,
    "mae_qc_vcf_tbi": context.mae_qc_vcf_tbi,
    "mae_qc_groups": context.mae_qc_groups,
    "mae_dna_rna_match_cutoff": to_number.call(context.mae_dna_rna_match_cutoff),
    "igenomes_ignore": to_bool.call(context.igenomes_ignore),
    "igenomes_base": context.igenomes_base
}

params.reject! { |_k, v| v.is_a?(String) && v.strip.empty? }
%><%= JSON.pretty_generate(params) %>
